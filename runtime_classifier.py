import pyspark


def evaluation(sc):
    """
    This pipeline predicts if an aircraft is going to go for unscheduled maintenance
    given a new record (<aircraft, date, FH, FC, DM, AVG(sensor)>).
    """

    """Given an aircraft and a day, replicates the data management pipeline and prepares 
    the tuple to be inputted into the model."""

    """Classifies the record and outputs maintenance / no maintenance."""

    pass
